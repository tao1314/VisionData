## 2026-09-23（工作记录-补充4）

一、问题概述

现有框选识别逐列选择灰度梯度最大像素，吸附描线在鼠标邻域内选择灰度梯度最大像素，容易被网格、坐标轴、文字和其他曲线干扰，存在跳线、抖动和断点问题。

二、处理内容

- 梳理两种识别方式的现有实现和共同误差来源。
- 制定“颜色分割 + 干扰抑制 + 连续路径搜索 + 人工校正”的优化方案。
- 给出框选识别、吸附描线、置信度交互、技术拆分、分阶段实施和量化验收建议。

三、验证结果（含涉及文件）

- 本次仅形成技术方案，未修改业务代码。
- 涉及文件：`曲线识别优化方案.md`、`工作记录.md`。

## 2026-09-23（工作记录-补充3）

一、问题概述

结果数据面板的“新增”按钮文字和图标受主题样式影响未保持白色，同时新增数据线的名称编辑入口不够明确。

二、处理内容

- 为“新增”主按钮显式设置白色文字、白色图标及水平垂直居中布局。
- 将数据线名称输入改为双向绑定，增加编辑图标、清空能力、长度限制和输入提示。
- 数据线名称修改后同步更新标签名称以及 XLSX 导出中的数据线名称。

三、验证结果（含涉及文件）

- 涉及文件：`frontend/src/components/recognition/CurvePanel.vue`、`frontend/src/assets/styles/index.scss`。
- `npm run build` 构建成功，Vite 完成 1688 个模块转换；名称计算属性的读写链路通过 Vue 编译校验。

## 2026-09-23（工作记录-补充2）

一、问题概述

识别工作区的图片导入入口位置、主导航展开方式及数据导出格式与实际使用要求不一致。

二、处理内容

- 将“导入图片”按钮移动到右侧工具栏最末端。
- 将最左侧主导航固定为 64px 图标模式，移除展开按钮。
- 移除“关于项目”菜单和路由入口。
- 引入 `xlsx`，将原 CSV 导出替换为包含“识别数据”工作表的 XLSX 文件，并设置列标题和列宽。

三、验证结果（含涉及文件）

- 涉及文件：`frontend/src/components/layout/AppSidebar.vue`、`frontend/src/router/index.js`、`frontend/src/views/recognition/index.vue`、`frontend/src/components/recognition/DataPreviewDialog.vue`、`frontend/src/assets/styles/index.scss`、`frontend/package.json`。
- 已执行依赖安装并更新 `frontend/package-lock.json`。
- `npm run build` 构建成功，Vite 完成 1688 个模块转换，XLSX 导出依赖已进入生产构建。

## 2026-09-23（工作记录-补充1）

一、问题概述

图片识别页原有结构以图片队列和结果空状态为主，无法满足多数据线管理、坐标轴标定、框选识别、吸附描线及数据预览导出的使用流程。

二、处理内容

- 将识别页调整为左右工作区：左侧数据配置与结果操作，右侧工具栏与图片画布。
- 支持新增、切换、删除多条数据线，并为每条数据线独立配置名称、颜色、XY 轴最大最小值。
- 支持在图片上拖动标注 X 轴、Y 轴位置，并按标定方向把像素坐标换算为业务数值。
- 增加框选识别和吸附描线两种曲线获取方式；吸附描线基于像素灰度梯度寻找邻域候选点。
- 增加撤销、清除选取、数据弹窗预览和 CSV 导出能力。
- 图片导入服务增加 Data URL 预览数据，避免渲染进程直接访问本地文件路径。

三、验证结果（含涉及文件）

- `npm run build` 构建成功，Vite 完成 1692 个模块转换。
- 使用 1440×900 桌面视口实际检查左右布局、工具栏、配置面板和画布空状态，显示正常。
- 浏览器控制台无 error 或 warning。
- 涉及文件：`electron/service/recognition/index.js`、`frontend/src/views/recognition/index.vue`、`frontend/src/components/recognition/*.vue`、`frontend/src/assets/styles/index.scss`。
